mutation SaveDiscoverArticle($input: SaveDiscoverArticleInput!) {
  saveDiscoverArticle(input: $input) {
    ... on SaveDiscoverArticleSuccess {
      saveId
      url
    }
    ... on SaveDiscoverArticleError {
      errorCodes
    }
  }
}
